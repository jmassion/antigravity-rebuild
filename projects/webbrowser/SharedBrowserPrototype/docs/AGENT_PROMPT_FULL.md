# Full Build Prompt (Paste into Claude/Codex/Cloud Opus)

Implement TabSpace World with:
- multiplayer permissions (server authoritative)
- client LOD + texture budgeting
- host heartbeats + capacity backpressure
- media plane (WS frames -> WebRTC)
- optional fleet scheduler + egress policy

Hard constraints:
- Never share cookies/credentials between users.
- Pixels + capability-gated control only.
- Minimize churn for authenticated sessions.

Deliver incrementally and keep runnable after each phase.

